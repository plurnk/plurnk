#compdef plurnk
# zsh completion for plurnk (generated by scripts/generate-posix.mjs)
local -a flags subs
flags=(--help --version --json --workspace --worker --model --reasoning --project-root --yolo --auto --flags --request_user_input --env-file --env-file-if-exists --max-turns --timeout --files-items --md --max-commands --no-git --loop --turn --since --limit --provider --all --offset --width)
subs=(models workspace log reasoning render mcp script)
if [[ $words[CURRENT] == -* ]]; then
    compadd -- $flags
elif (( CURRENT == 2 )); then
    compadd -- $subs
    _files
else
    _files
fi
